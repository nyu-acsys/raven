func sum(n: Int) returns (res: Int)
  requires n >= 0
{
  n <= 0 ? 0 : n + sum(n-1)
}

lemma sum_contract(n: Int)
  requires n >= 0
  ensures sum(n) >= n
  decreases n
{
  if (n > 0) {
    sum_contract(n-1);
  }
}
