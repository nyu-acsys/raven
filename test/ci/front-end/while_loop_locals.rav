field f: Int

proc p() {
    var z: Ref := new(f: 1);
    while(true)
        invariant own(z, f, 1, 1.0)
    {
        val p := true;
        var x: Bool;
        x := true;
        var y := z.f;
    }
}