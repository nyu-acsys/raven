module Bint {

}

modulse Lock {
      var x = true

      proc create() 
          returns (x : Bool) 
      {
          var z : Int = 1;
          var y : Bool = z.z;
      }

    proc acquire() {
        var x = create();
    }
  }