module Bint {
    type t = Int
    type t2 = Bool
}

module Lock {
    import Bint

      var x = true

      proc create() 
          returns (x : Bool) 
      {
          var z : Int = 1;
          var y : Bool = z.z;
      }

    proc acquire() {
        var x = create();
    }
  }