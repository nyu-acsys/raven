// Custom library to be included as part of this extension. The contents of this file are appended to Raven's `Library` module.

// This module is used to define a unique field and Ref location for use by this extension. Here we also see the use of the `admitted` keyword.
module ErrorCreds {
    field error_cred : Library.Fraction

    admitted val error_loc: Ref
}