// Exercise 1 solution: reset a counter to zero, regardless of its current value.
field count: Int

proc reset(c: Ref, implicit ghost v: Int)
  requires own(c.count, v)
  ensures own(c.count, 0)
{
  c.count := 0;
}
