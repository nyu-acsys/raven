// Exercise 1: reset a counter to zero, regardless of its current value.
// Fill in the body.
field count: Int

proc reset(c: Ref, implicit ghost v: Int)
  requires own(c.count, v)
  ensures own(c.count, 0)
{
  // TODO
}
