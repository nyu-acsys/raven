node_modules/
.vitepress/dist/
.vitepress/cache/

# Mirrored by scripts/sync-rav.mjs (predev/prebuild) from the .rav files
# already tracked under tutorial/**; regenerated, not source. They live under
# public/ so that the links in the prose (e.g. ./hit_counter_pure.rav) resolve
# to something the site actually serves.
public/**/*.rav
